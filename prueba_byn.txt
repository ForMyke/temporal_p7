<!DOCTYPE html>
<html lang="es">
<head>
	<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
	<meta charset="utf-8">
	<meta name="author" content="Carlos Pineda Guerrero. 2025,2026">
	<title>ShopFlow</title>

	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
	<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=IBM+Plex+Mono:wght@300;400;500&display=swap" rel="stylesheet">

	<style>
		:root {
			--black: #0a0a0a;
			--white: #fafafa;
			--gray: #f0f0f0;
			--gray-mid: #d0d0d0;
			--border: 2px solid #0a0a0a;
		}

		*, *::before, *::after { box-sizing: border-box; }

		body {
			background: var(--white);
			color: var(--black);
			font-family: 'IBM Plex Mono', monospace;
			font-weight: 400;
			min-height: 100vh;
		}

		/* ── Header ─────────────────────────────── */
		.app-header {
			background: var(--black);
			padding: 1rem 2rem;
			display: flex;
			align-items: center;
			gap: 1rem;
			border-bottom: 4px solid var(--black);
		}
		.brand-mark {
			width: 36px; height: 36px;
			background: var(--white);
			border: 2px solid var(--white);
			display: inline-flex;
			align-items: center;
			justify-content: center;
			color: var(--black);
			font-size: 1rem;
			flex-shrink: 0;
		}
		.brand-name {
			font-family: 'Playfair Display', serif;
			font-weight: 900;
			font-size: 1.4rem;
			color: var(--white);
			letter-spacing: 0.04em;
			text-transform: uppercase;
		}
		.brand-name em {
			font-style: normal;
			color: var(--gray-mid);
		}

		/* ── Layout ──────────────────────────────── */
		.app-content {
			display: flex;
			justify-content: center;
			padding: 3rem 1rem 4rem;
		}

		/* ── Panel ───────────────────────────────── */
		.panel {
			background: var(--white);
			border: var(--border);
			border-top: 4px solid var(--black);
			padding: 2.5rem 2rem;
			width: 100%;
			max-width: 480px;
			transition: box-shadow 0.2s;
		}
		.panel:hover {
			box-shadow: 6px 6px 0 var(--black);
		}
		.panel-wide { max-width: 580px; }

		.panel-title {
			font-family: 'Playfair Display', serif;
			font-weight: 900;
			font-size: 1.6rem;
			color: var(--black);
			margin-bottom: 2rem;
			padding-bottom: 1rem;
			border-bottom: var(--border);
			letter-spacing: -0.01em;
			line-height: 1.1;
		}
		.panel-title .title-num {
			font-family: 'IBM Plex Mono', monospace;
			font-size: 0.7rem;
			font-weight: 500;
			color: var(--gray-mid);
			display: block;
			letter-spacing: 0.15em;
			text-transform: uppercase;
			margin-bottom: 0.25rem;
		}

		/* ── Form controls ───────────────────────── */
		.form-label {
			font-size: 0.68rem;
			text-transform: uppercase;
			letter-spacing: 0.12em;
			color: var(--black);
			font-weight: 500;
			margin-bottom: 0.3rem;
			font-family: 'IBM Plex Mono', monospace;
		}
		.req { color: var(--black); font-weight: 700; }

		.form-control, .form-select {
			background: var(--gray) !important;
			border: var(--border) !important;
			border-radius: 0 !important;
			color: var(--black) !important;
			font-family: 'IBM Plex Mono', monospace !important;
			font-size: 0.85rem !important;
			padding: 0.65rem 0.85rem !important;
			transition: background 0.1s, box-shadow 0.1s;
		}
		.form-control:focus, .form-select:focus {
			background: var(--white) !important;
			box-shadow: 3px 3px 0 var(--black) !important;
			outline: none !important;
		}
		.form-control::placeholder {
			color: var(--gray-mid);
			font-family: 'IBM Plex Mono', monospace;
		}
		textarea.form-control { resize: vertical; }

		/* ── Buttons ─────────────────────────────── */
		.btn-solid {
			background: var(--black);
			color: var(--white);
			border: var(--border);
			border-radius: 0;
			font-family: 'IBM Plex Mono', monospace;
			font-weight: 500;
			font-size: 0.78rem;
			letter-spacing: 0.1em;
			text-transform: uppercase;
			padding: 0.75rem 1rem;
			width: 100%;
			cursor: pointer;
			transition: background 0.15s, color 0.15s, box-shadow 0.15s;
		}
		.btn-solid:hover {
			background: var(--white);
			color: var(--black);
			box-shadow: 4px 4px 0 var(--black);
		}

		.btn-outline {
			background: var(--white);
			color: var(--black);
			border: var(--border);
			border-radius: 0;
			font-family: 'IBM Plex Mono', monospace;
			font-weight: 500;
			font-size: 0.78rem;
			letter-spacing: 0.1em;
			text-transform: uppercase;
			padding: 0.75rem 1rem;
			width: 100%;
			cursor: pointer;
			transition: background 0.15s, color 0.15s, box-shadow 0.15s;
		}
		.btn-outline:hover {
			background: var(--black);
			color: var(--white);
			box-shadow: 4px 4px 0 var(--black);
		}

		.btn-ghost-bw {
			background: var(--gray);
			color: var(--black);
			border: 2px solid var(--gray-mid);
			border-radius: 0;
			font-family: 'IBM Plex Mono', monospace;
			font-weight: 400;
			font-size: 0.78rem;
			letter-spacing: 0.08em;
			text-transform: uppercase;
			padding: 0.75rem 1rem;
			width: 100%;
			cursor: pointer;
			transition: all 0.15s;
		}
		.btn-ghost-bw:hover {
			background: var(--black);
			color: var(--white);
			border-color: var(--black);
		}

		.btn-danger-bw {
			background: var(--white);
			color: var(--black);
			border: 2px solid var(--black);
			border-radius: 0;
			font-family: 'IBM Plex Mono', monospace;
			font-weight: 500;
			font-size: 0.78rem;
			letter-spacing: 0.1em;
			text-transform: uppercase;
			padding: 0.75rem 1rem;
			width: 100%;
			cursor: pointer;
			transition: all 0.15s;
			text-decoration: line-through;
		}
		.btn-danger-bw:hover {
			background: var(--black);
			color: var(--white);
			text-decoration: none;
			box-shadow: 4px 4px 0 var(--gray-mid);
		}

		/* ── Divider ─────────────────────────────── */
		.divider-bw {
			display: flex; align-items: center; gap: 1rem;
			color: var(--gray-mid);
			font-size: 0.68rem;
			letter-spacing: 0.12em;
			text-transform: uppercase;
			margin: 1.5rem 0;
			font-family: 'IBM Plex Mono', monospace;
		}
		.divider-bw::before, .divider-bw::after {
			content: ''; flex: 1; height: 1px; background: var(--gray-mid);
		}

		/* ── Menu grid ───────────────────────────── */
		.menu-grid {
			display: grid;
			grid-template-columns: 1fr 1fr;
			gap: 0;
			border: var(--border);
			margin-bottom: 0;
		}
		.menu-card {
			background: var(--white);
			border: none;
			border-right: var(--border);
			border-bottom: var(--border);
			padding: 1.5rem 1rem;
			text-align: center;
			cursor: pointer;
			transition: background 0.15s, color 0.15s;
			color: var(--black);
			font-family: 'IBM Plex Mono', monospace;
			font-weight: 500;
			font-size: 0.72rem;
			letter-spacing: 0.1em;
			text-transform: uppercase;
			display: flex;
			flex-direction: column;
			align-items: center;
			gap: 0.6rem;
		}
		.menu-card:nth-child(2n) { border-right: none; }
		.menu-card i { font-size: 1.4rem; }
		.menu-card:hover { background: var(--black); color: var(--white); }
		.menu-card.menu-card-full {
			grid-column: span 2;
			border-right: none;
			border-bottom: none;
			flex-direction: row;
			justify-content: center;
			gap: 0.75rem;
		}
		.menu-card.menu-card-full:hover {
			background: var(--gray);
			color: var(--black);
			text-decoration: line-through;
		}
		.menu-card.exit-card:hover {
			background: var(--gray);
			color: var(--black);
		}

		/* ── Photo upload ────────────────────────── */
		.photo-row {
			display: flex;
			align-items: center;
			gap: 1rem;
			background: var(--gray);
			border: var(--border);
			padding: 1rem;
		}
		.photo-thumb {
			width: 72px; height: 72px;
			object-fit: cover;
			border: var(--border);
			flex-shrink: 0;
			filter: grayscale(100%);
		}
		.photo-row .upload-col { flex: 1; }
		.photo-row small {
			color: var(--gray-mid);
			font-size: 0.68rem;
			font-family: 'IBM Plex Mono', monospace;
			letter-spacing: 0.05em;
		}

		/* ── Search bar ──────────────────────────── */
		.search-bar { display: flex; gap: 0; margin-bottom: 1rem; border: var(--border); }
		.search-bar .form-control {
			flex: 1;
			border: none !important;
			border-right: var(--border) !important;
		}
		.btn-search {
			background: var(--black);
			color: var(--white);
			border: none;
			padding: 0 1.25rem;
			font-family: 'IBM Plex Mono', monospace;
			font-weight: 500;
			font-size: 0.75rem;
			letter-spacing: 0.1em;
			text-transform: uppercase;
			cursor: pointer;
			white-space: nowrap;
			transition: background 0.15s, color 0.15s;
		}
		.btn-search:hover { background: var(--gray); color: var(--black); }

		.action-row { display: flex; gap: 0; margin-bottom: 1.5rem; border: var(--border); }
		.action-row .btn-solid,
		.action-row .btn-outline,
		.action-row .btn-ghost-bw {
			width: auto; flex: 1;
			border: none;
			border-right: var(--border);
		}
		.action-row > *:last-child { border-right: none; }

		/* ── Article cards (dynamic) ─────────────── */
		.art-card {
			background: var(--white);
			border: var(--border);
			border-top: none;
			padding: 1rem;
			display: flex;
			gap: 1rem;
			transition: box-shadow 0.15s;
		}
		.art-card:first-of-type { border-top: var(--border); }
		.art-card:hover { box-shadow: 4px 4px 0 var(--black); z-index: 1; position: relative; }
		.art-card img {
			width: 70px; height: 70px;
			object-fit: cover;
			border: var(--border);
			flex-shrink: 0;
			filter: grayscale(100%);
		}
		.art-card .art-body { flex: 1; }
		.art-card .art-name {
			font-family: 'Playfair Display', serif;
			font-weight: 700;
			font-size: 0.95rem;
			color: var(--black);
			margin-bottom: 0.2rem;
		}
		.art-card .art-desc {
			color: var(--gray-mid);
			font-size: 0.72rem;
			margin-bottom: 0.4rem;
			letter-spacing: 0.03em;
		}
		.art-card .art-price {
			font-family: 'Playfair Display', serif;
			font-weight: 700;
			font-size: 1rem;
			color: var(--black);
		}
		.art-card .art-buy { display: flex; align-items: center; gap: 0.5rem; margin-top: 0.5rem; }
		.art-qty {
			width: 60px;
			background: var(--gray);
			border: var(--border);
			border-radius: 0;
			color: var(--black);
			font-family: 'IBM Plex Mono', monospace;
			font-size: 0.8rem;
			padding: 0.3rem 0.5rem;
		}
		.btn-add-cart {
			background: var(--black);
			color: var(--white);
			border: var(--border);
			border-radius: 0;
			font-family: 'IBM Plex Mono', monospace;
			font-weight: 500;
			font-size: 0.72rem;
			letter-spacing: 0.08em;
			text-transform: uppercase;
			padding: 0.35rem 0.85rem;
			cursor: pointer;
			transition: background 0.15s, color 0.15s;
		}
		.btn-add-cart:hover { background: var(--white); color: var(--black); }

		/* ── Cart total ──────────────────────────── */
		.cart-total {
			background: var(--black);
			color: var(--white);
			padding: 1rem 1.25rem;
			font-family: 'Playfair Display', serif;
			font-weight: 900;
			font-size: 1.2rem;
			text-align: right;
			margin-top: 0;
			border: var(--border);
			border-top: none;
			letter-spacing: -0.01em;
		}
		.btn-remove {
			background: var(--white);
			color: var(--black);
			border: var(--border);
			border-radius: 0;
			font-family: 'IBM Plex Mono', monospace;
			font-size: 0.68rem;
			letter-spacing: 0.08em;
			text-transform: uppercase;
			padding: 0.25rem 0.6rem;
			cursor: pointer;
			transition: all 0.15s;
			text-decoration: line-through;
		}
		.btn-remove:hover { background: var(--black); color: var(--white); text-decoration: none; }

		.mb-f { margin-bottom: 1rem; }

		@keyframes fadeUp {
			from { opacity: 0; transform: translateY(12px); }
			to   { opacity: 1; transform: translateY(0); }
		}
		.panel { animation: fadeUp 0.3s ease both; }
	</style>

	<script src='/WSClient.js'></script>
	<script>
		var URL = "/Servicio/rest/ws";
		var id_usuario;
		var token;
		var foto = null;

		function get(id) { return document.getElementById(id); }
		function muestra(id) { get(id).style.display = "block"; }
		function oculta(id) { get(id).style.display = "none"; }

		function readSingleFile(files, imagen)
		{
			var file = files[0];
			if (!file) return;
			var reader = new FileReader();
			reader.onload = function(e) { imagen.src = reader.result; foto = reader.result.split(',')[1]; };
			reader.readAsDataURL(file);
		}

		function sha256(ascii)
		{
			function rightRotate(value, amount) { return (value >>> amount) | (value << (32 - amount)); }
			var mathPow = Math.pow;
			var maxWord = mathPow(2, 32);
			var lengthProperty = 'length';
			var i, j;
			var result = '';
			var words = [];
			var asciiBitLength = ascii[lengthProperty] * 8;
			var hash = sha256.h = sha256.h || [];
			var k = sha256.k = sha256.k || [];
			var primeCounter = k[lengthProperty];
			var isComposite = {};
			for (var candidate = 2; primeCounter < 64; candidate++)
			{
				if (!isComposite[candidate])
				{
					for (i = 0; i < 313; i += candidate) isComposite[i] = candidate;
					hash[primeCounter] = (mathPow(candidate, .5) * maxWord) | 0;
					k[primeCounter++] = (mathPow(candidate, 1 / 3) * maxWord) | 0;
				}
			}
			ascii += '\x80';
			while (ascii[lengthProperty] % 64 - 56) ascii += '\x00';
			for (i = 0; i < ascii[lengthProperty]; i++)
			{
				j = ascii.charCodeAt(i);
				if (j >> 8) throw new Error('Solo se soporta ASCII');
				words[i >> 2] |= j << ((3 - i) % 4) * 8;
			}
			words[words[lengthProperty]] = ((asciiBitLength / maxWord) | 0);
			words[words[lengthProperty]] = (asciiBitLength);
			for (j = 0; j < words[lengthProperty];)
			{
				var w = words.slice(j, j += 16);
				var oldHash = hash.slice(0);
				for (i = 0; i < 64; i++)
				{
					var w15 = w[i-15], w2 = w[i-2], a = hash[0], e = hash[4];
					var temp1 = hash[7]
						+ (rightRotate(e,6) ^ rightRotate(e,11) ^ rightRotate(e,25))
						+ ((e & hash[5]) ^ ((~e) & hash[6])) + k[i]
						+ (w[i] = (i < 16) ? w[i] : (
							w[i-16] + (rightRotate(w15,7) ^ rightRotate(w15,18) ^ (w15>>>3))
							+ w[i-7] + (rightRotate(w2,17) ^ rightRotate(w2,19) ^ (w2>>>10))
						) | 0);
					var temp2 = (rightRotate(a,2) ^ rightRotate(a,13) ^ rightRotate(a,22))
						+ ((a & hash[1]) ^ (a & hash[2]) ^ (hash[1] & hash[2]));
					hash = [(temp1 + temp2) | 0].concat(hash);
					hash[4] = (hash[4] + temp1) | 0;
					hash.pop();
				}
				for (i = 0; i < 8; i++) hash[i] = (hash[i] + oldHash[i]) | 0;
			}
			for (i = 0; i < 8; i++)
				for (j = 3; j+1; j--) { var b = (hash[i] >> (j*8)) & 255; result += ((b < 16) ? 0 : '') + b.toString(16); }
			return result;
		}

		function limpia_login() { get("login_email").value = ""; get("login_password").value = ""; }

		function login()
		{
			var cliente = new WSClient(URL);
			var password = get("login_password").value;
			cliente.get("login", { email: get("login_email").value, password: sha256(password) },
			function(code, result)
			{
				if (code == 200) { id_usuario = result.id_usuario; token = result.token; oculta("login"); muestra("menu"); }
				else alert(JSON.stringify(result));
			});
		}

		function limpia_alta()
		{
			get("alta_email").value = ""; get("alta_password").value = ""; get("alta_nombre").value = "";
			get("alta_apellido_paterno").value = ""; get("alta_apellido_materno").value = "";
			get("alta_fecha_nacimiento").value = ""; get("alta_telefono").value = "";
			get("alta_genero").value = ""; get("alta_imagen").src = "/usuario_sin_foto.png"; foto = null;
		}

		function alta()
		{
			var cliente = new WSClient(URL);
			var password = get("alta_password").value;
			var usuario = {
				email: get("alta_email").value,
				password: password != "" ? sha256(password) : password,
				nombre: get("alta_nombre").value,
				apellido_paterno: get("alta_apellido_paterno").value,
				apellido_materno: get("alta_apellido_materno").value != "" ? get("alta_apellido_materno").value : null,
				fecha_nacimiento: get("alta_fecha_nacimiento").value != "" ? new Date(get("alta_fecha_nacimiento").value).toISOString() : null,
				telefono: get("alta_telefono").value != "" ? get("alta_telefono").value : null,
				genero: get("alta_genero").value == "Masculino" ? "M" : get("alta_genero").value == "Femenino" ? "F" : null,
				foto: foto
			};
			cliente.post("alta_usuario", usuario, function(code, result)
			{
				if (code == 200) { alert("Se registró el usuario"); limpia_login(); oculta('alta_usuario'); muestra('login'); }
				else alert(JSON.stringify(result));
			});
		}

		function limpia_consulta()
		{
			get("consulta_email").value = ""; get("consulta_password").value = ""; get("consulta_nombre").value = "";
			get("consulta_apellido_paterno").value = ""; get("consulta_apellido_materno").value = "";
			get("consulta_fecha_nacimiento").value = ""; get("consulta_telefono").value = "";
			get("consulta_genero").value = ""; get("consulta_imagen").src = "/usuario_sin_foto.png";
		}

		function quita_foto() { foto = null; get('consulta_imagen').src = 'usuario_sin_foto.png'; get('consulta_file').value = ''; }

		function formatearFecha(fecha)
		{
			var fecha = new Date(fecha);
			var año = fecha.getFullYear();
			var mes = (fecha.getMonth() + 1).toString().padStart(2,'0');
			var dia = fecha.getDate().toString().padStart(2,'0');
			var horas = fecha.getHours().toString().padStart(2,'0');
			var minutos = fecha.getMinutes().toString().padStart(2,'0');
			var segundos = fecha.getSeconds().toString().padStart(2,'0');
			return año + "-" + mes + "-" + dia + "T" + horas + ":" + minutos + ":" + segundos;
		}

		function consulta()
		{
			var cliente = new WSClient(URL);
			cliente.get("consulta_usuario", { id_usuario: id_usuario, token: token },
			function(code, result)
			{
				if (code == 200)
				{
					limpia_consulta(); oculta('menu'); muestra('consulta_usuario');
					get("consulta_email").value = result.email;
					get("consulta_password").value = "";
					get("consulta_nombre").value = result.nombre;
					get("consulta_apellido_paterno").value = result.apellido_paterno;
					get("consulta_apellido_materno").value = result.apellido_materno != null ? result.apellido_materno : "";
					get("consulta_fecha_nacimiento").value = formatearFecha(new Date(result.fecha_nacimiento));
					get("consulta_telefono").value = result.telefono != null ? result.telefono : "";
					get("consulta_genero").value = result.genero == "M" ? "Masculino" : result.genero == "F" ? "Femenino" : "";
					foto = result.foto;
					get("consulta_imagen").src = foto != null ? "data:image/jpeg;base64," + foto : "/usuario_sin_foto.png";
				}
				else alert(JSON.stringify(result));
			});
		}

		function modifica()
		{
			var cliente = new WSClient(URL);
			var password = get("consulta_password").value;
			var usuario = {
				email: get("consulta_email").value,
				password: password != "" ? sha256(password) : password,
				nombre: get("consulta_nombre").value,
				apellido_paterno: get("consulta_apellido_paterno").value,
				apellido_materno: get("consulta_apellido_materno").value != "" ? get("consulta_apellido_materno").value : null,
				fecha_nacimiento: get("consulta_fecha_nacimiento").value != "" ? new Date(get("consulta_fecha_nacimiento").value).toISOString() : null,
				telefono: get("consulta_telefono").value != "" ? get("consulta_telefono").value : null,
				genero: get("consulta_genero").value == "Masculino" ? "M" : get("consulta_genero").value == "Femenino" ? "F" : null,
				foto: foto
			};
			cliente.put("modifica_usuario", { id_usuario: id_usuario, token: token }, usuario,
			function(code, result)
			{
				if (code == 200) alert("Se modificó el perfil del usuario");
				else alert(JSON.stringify(result));
			});
		}

		function borra()
		{
			var client = new WSClient(URL);
			client.delete("borra_usuario", { id_usuario: id_usuario, token: token },
			function(code, result)
			{
				if (code == 200) { alert("Se eliminó el perfil del usuario"); limpia_login(); oculta('menu'); muestra('login'); }
				else alert(JSON.stringify(result));
			});
		}

		var foto_articulo = null;
		var resultados = [];
		var carrito = [];

		function readArticuloFile(files, imagen)
		{
			var file = files[0];
			if (!file) return;
			var reader = new FileReader();
			reader.onload = function(e) { imagen.src = reader.result; foto_articulo = reader.result.split(',')[1]; };
			reader.readAsDataURL(file);
		}

		function limpia_captura_articulo()
		{
			get("art_nombre").value = ""; get("art_descripcion").value = "";
			get("art_precio").value = ""; get("art_cantidad").value = "";
			get("art_imagen").src = "/usuario_sin_foto.png"; foto_articulo = null;
		}

		function captura_articulo()
		{
			var articulo = {
				nombre: get("art_nombre").value,
				descripcion: get("art_descripcion").value != "" ? get("art_descripcion").value : null,
				precio: get("art_precio").value != "" ? parseFloat(get("art_precio").value) : null,
				cantidad: get("art_cantidad").value != "" ? parseInt(get("art_cantidad").value) : null,
				foto: foto_articulo
			};
			fetch(URL + "/alta_articulo?id_usuario=" + id_usuario + "&token=" + encodeURIComponent(token),
			{ method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(articulo) })
			.then(function(r) { return r.json().then(function(data) { if (r.status == 200) { alert("Se dio de alta el artículo"); limpia_captura_articulo(); } else alert(JSON.stringify(data)); }); })
			.catch(function(e) { alert("Error: " + e.message); });
		}

		function escHtml(s) { if (!s) return ""; return s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;"); }

		function busca_articulos()
		{
			var cliente = new WSClient(URL);
			cliente.get("consulta_articulos", { palabra: get("busqueda_palabra").value, id_usuario: id_usuario, token: token },
			function(code, result)
			{
				if (code == 200)
				{
					resultados = result;
					var html = "";
					for (var i = 0; i < result.length; i++)
					{
						var a = result[i];
						var fotoSrc = a.foto ? "data:image/jpeg;base64," + a.foto : "/usuario_sin_foto.png";
						html += "<div class='art-card'><img src='" + fotoSrc + "' alt=''>";
						html += "<div class='art-body'><div class='art-name'>" + escHtml(a.nombre) + "</div>";
						html += "<div class='art-desc'>" + escHtml(a.descripcion) + "</div>";
						html += "<div class='art-price'>$" + (a.precio != null ? a.precio.toFixed(2) : "0.00") + "</div>";
						html += "<div class='art-buy'><input type='number' id='cant_" + i + "' value='1' min='1' class='art-qty'>";
						html += "<button class='btn-add-cart' onclick='compra(" + i + ")'><i class='bi bi-cart-plus'></i> Comprar</button></div></div></div>";
					}
					get("resultados_articulos").innerHTML = html || "<p style='font-size:0.78rem;color:#d0d0d0;letter-spacing:0.08em;text-transform:uppercase;margin-top:1rem;'>— Sin resultados —</p>";
				}
				else alert(JSON.stringify(result));
			});
		}

		function compra(idx)
		{
			var a = resultados[idx];
			var cantidad = parseInt(get("cant_" + idx).value);
			if (isNaN(cantidad) || cantidad < 1) { alert("La cantidad debe ser mayor a 0"); return; }
			var cliente = new WSClient(URL);
			cliente.put("compra_articulo", { id_articulo: a.id_articulo, cantidad: cantidad, id_usuario: id_usuario, token: token }, {},
			function(code, result)
			{
				if (code == 200)
				{
					var encontrado = false;
					for (var i = 0; i < carrito.length; i++) { if (carrito[i].id_articulo == a.id_articulo) { carrito[i].cantidad += cantidad; encontrado = true; break; } }
					if (!encontrado) carrito.push({ id_articulo: a.id_articulo, nombre: a.nombre, descripcion: a.descripcion, precio: a.precio, cantidad: cantidad, foto: a.foto });
					alert("Artículo agregado al carrito");
				}
				else alert(JSON.stringify(result));
			});
		}

		function renderiza_carrito()
		{
			var html = "";
			var total = 0;
			for (var i = 0; i < carrito.length; i++)
			{
				var a = carrito[i];
				var costo = a.precio * a.cantidad;
				total += costo;
				var fotoSrc = a.foto ? "data:image/jpeg;base64," + a.foto : "/usuario_sin_foto.png";
				html += "<div class='art-card'><img src='" + fotoSrc + "' alt=''>";
				html += "<div class='art-body'><div class='art-name'>" + escHtml(a.nombre) + "</div>";
				html += "<div class='art-desc'>" + a.cantidad + " × $" + a.precio.toFixed(2) + "</div>";
				html += "<div class='art-price'>$" + costo.toFixed(2) + "</div>";
				html += "<div class='art-buy'><button class='btn-remove' onclick='elimina_del_carrito(" + a.id_articulo + ")'><i class='bi bi-x'></i> Eliminar</button></div></div></div>";
			}
			if (carrito.length == 0) html = "<p style='font-size:0.78rem;color:#d0d0d0;letter-spacing:0.08em;text-transform:uppercase;padding:1rem 0;'>— Carrito vacío —</p>";
			html += "<div class='cart-total'>Total &nbsp; $" + total.toFixed(2) + "</div>";
			get("lista_carrito").innerHTML = html;
		}

		function ir_a_carrito() { renderiza_carrito(); oculta("compra_articulos"); muestra("articulos_carrito"); }

		function elimina_del_carrito(id_articulo)
		{
			var cliente = new WSClient(URL);
			cliente.delete("elimina_articulo_carrito_compra", { id_usuario: id_usuario, id_articulo: id_articulo, token: token },
			function(code, result)
			{
				if (code == 200) { carrito = carrito.filter(function(a) { return a.id_articulo != id_articulo; }); renderiza_carrito(); }
				else alert(JSON.stringify(result));
			});
		}

		function elimina_todo_carrito()
		{
			if (!confirm("¿Deseas eliminar todos los artículos del carrito?")) return;
			var cliente = new WSClient(URL);
			cliente.delete("elimina_carrito_compra", { id_usuario: id_usuario, token: token },
			function(code, result)
			{
				if (code == 200) { carrito = []; renderiza_carrito(); }
				else alert(JSON.stringify(result));
			});
		}
	</script>
</head>
<body>

<header class="app-header">
	<div class="brand-mark"><i class="bi bi-bag-fill"></i></div>
	<span class="brand-name">Shop<em>Flow</em></span>
</header>

<div class="app-content">

	<!-- LOGIN -->
	<div id="login" class="panel">
		<div class="panel-title">
			<span class="title-num">01 / Acceso</span>
			Bienvenido de nuevo
		</div>
		<div class="mb-f">
			<label class="form-label">Email</label>
			<input type="email" id="login_email" class="form-control" placeholder="correo@ejemplo.com">
		</div>
		<div class="mb-f">
			<label class="form-label">Contraseña</label>
			<input type="password" id="login_password" class="form-control" placeholder="••••••••">
		</div>
		<button type="button" onclick="login()" class="btn-solid mb-2">
			<i class="bi bi-arrow-right me-2"></i>Entrar
		</button>
		<div class="divider-bw">nueva cuenta</div>
		<button type="button" onclick="limpia_alta();oculta('login');muestra('alta_usuario')" class="btn-outline">
			<i class="bi bi-person-plus me-2"></i>Registro de usuario
		</button>
	</div>

	<!-- ALTA USUARIO -->
	<div id="alta_usuario" style="display:none" class="panel">
		<div class="panel-title">
			<span class="title-num">02 / Registro</span>
			Crear cuenta
		</div>
		<div class="mb-f">
			<label class="form-label">Email <span class="req">*</span></label>
			<input type="email" id="alta_email" class="form-control">
		</div>
		<div class="mb-f">
			<label class="form-label">Contraseña <span class="req">*</span></label>
			<input type="password" id="alta_password" class="form-control">
		</div>
		<div class="mb-f">
			<label class="form-label">Nombre <span class="req">*</span></label>
			<input type="text" id="alta_nombre" class="form-control">
		</div>
		<div class="row g-2 mb-f">
			<div class="col-6">
				<label class="form-label">Apellido paterno <span class="req">*</span></label>
				<input type="text" id="alta_apellido_paterno" class="form-control">
			</div>
			<div class="col-6">
				<label class="form-label">Apellido materno</label>
				<input type="text" id="alta_apellido_materno" class="form-control">
			</div>
		</div>
		<div class="mb-f">
			<label class="form-label">Fecha de nacimiento <span class="req">*</span></label>
			<input type="datetime-local" id="alta_fecha_nacimiento" class="form-control">
		</div>
		<div class="row g-2 mb-f">
			<div class="col-6">
				<label class="form-label">Teléfono</label>
				<input type="number" id="alta_telefono" class="form-control">
			</div>
			<div class="col-6">
				<label class="form-label">Género</label>
				<select id="alta_genero" class="form-select">
					<option></option><option>Masculino</option><option>Femenino</option>
				</select>
			</div>
		</div>
		<div class="mb-f">
			<label class="form-label">Foto de perfil</label>
			<div class="photo-row">
				<img id="alta_imagen" class="photo-thumb" src="/usuario_sin_foto.png">
				<div class="upload-col">
					<input type="file" class="form-control form-control-sm mb-1" onchange="readSingleFile(files,get('alta_imagen'))" multiple="false" accept="image/*">
					<small>JPG / PNG</small>
				</div>
			</div>
		</div>
		<button type="button" onclick="alta();" class="btn-solid mb-2">
			<i class="bi bi-check me-2"></i>Registrar usuario
		</button>
		<button type="button" onclick="limpia_login();oculta('alta_usuario');muestra('login')" class="btn-ghost-bw">
			<i class="bi bi-x me-2"></i>Cancelar
		</button>
	</div>

	<!-- MENÚ PRINCIPAL -->
	<div id="menu" style="display:none" class="panel">
		<div class="panel-title">
			<span class="title-num">03 / Navegación</span>
			Menú principal
		</div>
		<div class="menu-grid">
			<button class="menu-card" onclick="consulta();">
				<i class="bi bi-person"></i>Perfil
			</button>
			<button class="menu-card" onclick="limpia_captura_articulo();oculta('menu');muestra('captura_articulo')">
				<i class="bi bi-plus"></i>Artículo
			</button>
			<button class="menu-card" onclick="get('busqueda_palabra').value='';get('resultados_articulos').innerHTML='';oculta('menu');muestra('compra_articulos')">
				<i class="bi bi-grid"></i>Tienda
			</button>
			<button class="menu-card exit-card" onclick="limpia_login();carrito=[];resultados=[];oculta('menu');muestra('login')">
				<i class="bi bi-arrow-right"></i>Salir
			</button>
			<button class="menu-card menu-card-full" onclick="if(confirm('¿Deseas eliminar el perfil del usuario?')){borra();}">
				<i class="bi bi-trash"></i>Eliminar perfil
			</button>
		</div>
	</div>

	<!-- PERFIL USUARIO -->
	<div id="consulta_usuario" style="display:none" class="panel">
		<div class="panel-title">
			<span class="title-num">04 / Perfil</span>
			Datos del usuario
		</div>
		<div class="mb-f">
			<label class="form-label">Email <span class="req">*</span></label>
			<input type="email" id="consulta_email" class="form-control">
		</div>
		<div class="mb-f">
			<label class="form-label">Nueva contraseña</label>
			<input type="password" id="consulta_password" class="form-control" placeholder="Dejar vacío para no cambiar">
		</div>
		<div class="mb-f">
			<label class="form-label">Nombre <span class="req">*</span></label>
			<input type="text" id="consulta_nombre" class="form-control">
		</div>
		<div class="row g-2 mb-f">
			<div class="col-6">
				<label class="form-label">Apellido paterno <span class="req">*</span></label>
				<input type="text" id="consulta_apellido_paterno" class="form-control">
			</div>
			<div class="col-6">
				<label class="form-label">Apellido materno</label>
				<input type="text" id="consulta_apellido_materno" class="form-control">
			</div>
		</div>
		<div class="mb-f">
			<label class="form-label">Fecha de nacimiento <span class="req">*</span></label>
			<input type="datetime-local" id="consulta_fecha_nacimiento" class="form-control">
		</div>
		<div class="row g-2 mb-f">
			<div class="col-6">
				<label class="form-label">Teléfono</label>
				<input type="number" id="consulta_telefono" class="form-control">
			</div>
			<div class="col-6">
				<label class="form-label">Género</label>
				<select id="consulta_genero" class="form-select">
					<option></option><option>Masculino</option><option>Femenino</option>
				</select>
			</div>
		</div>
		<div class="mb-f">
			<label class="form-label">Foto de perfil</label>
			<div class="photo-row">
				<img id="consulta_imagen" class="photo-thumb" src="/usuario_sin_foto.png">
				<div class="upload-col">
					<input type="file" id="consulta_file" class="form-control form-control-sm mb-1" onchange="readSingleFile(files,get('consulta_imagen'))" multiple="false" accept="image/*">
					<button onclick="quita_foto()" style="background:none;border:none;font-family:'IBM Plex Mono',monospace;font-size:0.68rem;letter-spacing:0.08em;text-transform:uppercase;text-decoration:line-through;cursor:pointer;padding:0;color:#0a0a0a;">
						Quitar foto
					</button>
				</div>
			</div>
		</div>
		<button type="button" onclick="modifica()" class="btn-solid mb-2">
			<i class="bi bi-check me-2"></i>Guardar cambios
		</button>
		<button type="button" onclick="oculta('consulta_usuario');muestra('menu')" class="btn-ghost-bw">
			<i class="bi bi-arrow-left me-2"></i>Regresar
		</button>
	</div>

	<!-- CAPTURA ARTÍCULO -->
	<div id="captura_articulo" style="display:none" class="panel">
		<div class="panel-title">
			<span class="title-num">05 / Catálogo</span>
			Nuevo artículo
		</div>
		<div class="mb-f">
			<label class="form-label">Nombre <span class="req">*</span></label>
			<input type="text" id="art_nombre" class="form-control">
		</div>
		<div class="mb-f">
			<label class="form-label">Descripción</label>
			<textarea id="art_descripcion" class="form-control" rows="3"></textarea>
		</div>
		<div class="row g-2 mb-f">
			<div class="col-6">
				<label class="form-label">Precio <span class="req">*</span></label>
				<input type="number" id="art_precio" step="0.01" min="0" class="form-control">
			</div>
			<div class="col-6">
				<label class="form-label">Cantidad <span class="req">*</span></label>
				<input type="number" id="art_cantidad" min="0" class="form-control">
			</div>
		</div>
		<div class="mb-f">
			<label class="form-label">Foto del artículo</label>
			<div class="photo-row">
				<img id="art_imagen" class="photo-thumb" src="/usuario_sin_foto.png">
				<div class="upload-col">
					<input type="file" class="form-control form-control-sm mb-1" onchange="readArticuloFile(files,get('art_imagen'))" multiple="false" accept="image/*">
					<small>JPG / PNG</small>
				</div>
			</div>
		</div>
		<button type="button" onclick="captura_articulo()" class="btn-solid mb-2">
			<i class="bi bi-upload me-2"></i>Guardar artículo
		</button>
		<button type="button" onclick="limpia_captura_articulo();oculta('captura_articulo');muestra('menu')" class="btn-ghost-bw">
			<i class="bi bi-arrow-left me-2"></i>Regresar
		</button>
	</div>

	<!-- COMPRA ARTÍCULOS -->
	<div id="compra_articulos" style="display:none" class="panel panel-wide">
		<div class="panel-title">
			<span class="title-num">06 / Tienda</span>
			Compra de artículos
		</div>
		<div class="search-bar">
			<input type="text" id="busqueda_palabra" class="form-control" placeholder="Buscar artículo...">
			<button onclick="busca_articulos()" class="btn-search"><i class="bi bi-search me-1"></i>Buscar</button>
		</div>
		<div class="action-row">
			<button type="button" onclick="ir_a_carrito()" class="btn-outline">
				<i class="bi bi-cart me-1"></i>Mi carrito
			</button>
			<button type="button" onclick="oculta('compra_articulos');muestra('menu')" class="btn-ghost-bw">
				<i class="bi bi-arrow-left me-1"></i>Regresar
			</button>
		</div>
		<div id="resultados_articulos"></div>
	</div>

	<!-- CARRITO -->
	<div id="articulos_carrito" style="display:none" class="panel panel-wide">
		<div class="panel-title">
			<span class="title-num">07 / Carrito</span>
			Artículos seleccionados
		</div>
		<div id="lista_carrito"></div>
		<div class="action-row mt-3">
			<button type="button" onclick="elimina_todo_carrito()" class="btn-danger-bw">
				<i class="bi bi-trash me-1"></i>Vaciar carrito
			</button>
			<button type="button" onclick="oculta('articulos_carrito');muestra('compra_articulos')" class="btn-ghost-bw">
				<i class="bi bi-arrow-left me-1"></i>Seguir comprando
			</button>
		</div>
	</div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
